import 'package:flutter/material.dart';

const APP_NAME = "Smart Blood Donor";

const Color primaryColor = Color(0xFF75d330);
const Color primaryTextColor = Color(0xFF000000);
const Color accentColor = Colors.grey;

const double textScaleFactor = 1.1;